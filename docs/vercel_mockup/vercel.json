{
  "framework": "vite",
  "buildCommand": "vite build",
  "outputDirectory": "dist",
  "installCommand": "npm install",
  "ignoreCommand": "[ -n \"$VERCEL_GIT_PREVIOUS_SHA\" ] && git diff --quiet $VERCEL_GIT_PREVIOUS_SHA HEAD -- src index.html public package.json package-lock.json vite.config.js",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
