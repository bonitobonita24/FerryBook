{
  "framework": "vite",
  "buildCommand": "vite build",
  "outputDirectory": "dist",
  "installCommand": "npm install",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD -- src index.html public package.json package-lock.json vite.config.js",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
