# Design Identity — F and S Marine Transport Inc.
Source: Airbnb from getdesign.md
Generated by: Planning Assistant Phase 2.8 (V31) · last revised after Batch 16 (Seat-Number Threading + Step-Indicator Harmonization)
Install command: `npx getdesign@latest add airbnb` (run from project root after Phase 3 generates inputs.yml)

## Visual Theme

Travel-marketplace aesthetic anchored by a warm coral primary color that signals friendly action without aggression. Photography-driven hero areas dominate the customer-facing surface; admin and back-office areas use the same color tokens but lean on whitespace and typography rather than imagery. Generous breathing room, soft shadows on cards, and pill-shaped interactive elements give the interface a consumer-app feel that distances F and S Marine Transport Inc. from the typical clunky enterprise booking portals (2GO Online, PHBus) that dominate the Philippine ferry market. Trust signals — MARINA license badges, total-passengers-served counters, safety record — are styled as muted secondary elements so they support without competing.

## Color Palette

### Core tokens (shadcn/ui-compatible)

| Token              | HSL Value              | Hex      | Usage                                          |
|--------------------|------------------------|----------|------------------------------------------------|
| background         | hsl(0 0% 100%)         | #FFFFFF  | Page background, primary surface               |
| foreground         | hsl(0 0% 13%)          | #222222  | Primary text                                   |
| primary            | hsl(358 67% 51%)       | #FF385C  | Coral — primary CTAs, active states, brand     |
| primary-foreground | hsl(0 0% 100%)         | #FFFFFF  | Text on coral primary                          |
| primary-hover      | hsl(358 64% 45%)       | #E31C5F  | Hover state on coral CTAs                      |
| secondary          | hsl(0 0% 13%)          | #222222  | Secondary buttons, ink-dark CTAs               |
| secondary-foreground | hsl(0 0% 100%)       | #FFFFFF  | Text on secondary                              |
| muted              | hsl(0 0% 96%)          | #F7F7F7  | Subtle surfaces, disabled states               |
| muted-foreground   | hsl(0 0% 45%)          | #717171  | Secondary text, captions, hints                |
| accent             | hsl(0 0% 95%)          | #F2F2F2  | Hover states on neutral elements               |
| accent-foreground  | hsl(0 0% 13%)          | #222222  | Text on accent                                 |
| destructive        | hsl(0 84% 60%)         | #E63946  | Errors, delete actions, refund alerts, emergency cancellations |
| destructive-foreground | hsl(0 0% 100%)     | #FFFFFF  | Text on destructive                            |
| success            | hsl(160 51% 35%)       | #2A8364  | Confirmed bookings, paid status, ID-verified passengers |
| success-foreground | hsl(0 0% 100%)         | #FFFFFF  | Text on success                                |
| warning            | hsl(35 91% 50%)        | #F5A623  | Pending payment, no-show grace period, ID-verification pending |
| border             | hsl(0 0% 89%)          | #E4E4E4  | Card borders, input borders, dividers          |
| input              | hsl(0 0% 89%)          | #E4E4E4  | Input field borders (matches border)           |
| ring               | hsl(358 67% 51%)       | #FF385C  | Focus ring (coral)                             |
| card               | hsl(0 0% 100%)         | #FFFFFF  | Card backgrounds (raised on muted bg)          |
| card-foreground    | hsl(0 0% 13%)          | #222222  | Card text                                      |

### Locked architectural colors (DO NOT REASSIGN)

These are not theme tokens — they encode domain meaning that the UI relies on to communicate state at a glance. Phase 4 must preserve them exactly.

**Class colors** — every vessel class is identified by a fixed color across the booking flow, e-tickets, manifests, and admin reports:

| Class    | Foreground | Background tint | Usage                                     |
|----------|------------|-----------------|-------------------------------------------|
| Open Air | `#1E40AF`  | `#DBEAFE`       | Class cards, seat badges, fare breakdowns |
| Aircon   | `#FF385C` (= primary) | `#FFE5E9` | Class cards, seat badges, fare breakdowns |
| VIP      | `#A16207`  | `#FEF3C7`       | Class cards, seat badges, fare breakdowns |

**Port colors** — applied to port chips, schedule grids, and Daily Sales Report rows:

| Port            | Code    | Foreground | Background tint | Notes                                  |
|-----------------|---------|------------|-----------------|----------------------------------------|
| Nasugbu Port    | BAT-NAS | `#FF385C` (= primary, coral) | `#FFE5E9` | Origin-side, Batangas mainland |
| Calatagan Port  | BAT-CAL | `#A16207` (amber) | `#FEF3C7` | Origin-side, Batangas mainland |
| Tilik Port      | MIN-TIL | `#4B5563` (gray)  | `#F3F4F6` | Destination, Lubang Island |

**Role colors** — admin user badges, manifest signatures, audit log severity:

| Role               | Foreground | Background tint | Usage                                  |
|--------------------|------------|-----------------|----------------------------------------|
| Super Admin        | `#E63946` (= destructive) | `#FEE2E2` | Highest privilege; rare actions like API rotation |
| Operations Manager | `#1E40AF` (deep blue) | `#DBEAFE` | Schedules, ports, vessels, emergency broadcasts |
| Finance Manager    | `#2A8364` (= success) | `#DCFCE7` | Refunds, fares, reports, credit ledger |
| Ticketing Staff    | `#717171` (= ink-muted) | `#F7F7F7` | Walk-in booking, counter check-in |
| Boarding Officer   | `#7C3AED` (purple) | `#EDE9FE` | Gangway scan, MC-180 manifest signing — port-scoped |
| Customer           | `#222222` (= ink) | `#F7F7F7` | Default — used for non-admin contexts |

**Passenger-type pills (discount-ID verification)** — counter check-in surfaces a colored pill per passenger so staff can scan the manifest visually before scanning QRs:

| Passenger Type | Foreground | Background | Label                |
|----------------|------------|------------|----------------------|
| Adult          | `#717171` (= ink-muted) | `#F7F7F7` | "Adult" |
| Senior         | `#92400E` | `#FEF3C7` | "Senior · 20%" (RA 9994) |
| PWD            | `#5B21B6` | `#EDE9FE` | "PWD · 20%" (RA 10754) |
| Student        | `#1E40AF` | `#DBEAFE` | "Student · 20%" |
| Child          | `#9B1A3D` | `#FFE5E9` | "Child · 50%" (3-12y) |
| Infant         | `#9B1A3D` | `#FFE5E9` | "Infant · free" (0-3y) |

**Recovery-mode flow colors** — different cancellation/recovery paths are color-coded so customers can tell at a glance which they're in:

| Mode                              | Lead color | Tint     | Used in                                  |
|-----------------------------------|------------|----------|------------------------------------------|
| Pre-departure refund              | `#F5A623` (= warning) | `#FEF3C7` | Operator-favorable 6-tier ladder (50% cap, drops 10pp/day in last 5 days, 0% at <24h) — tone gets more destructive as the tier degrades |
| Pre-departure reschedule (paid)   | `#FF385C` (= primary) | `#FFE5E9` | Same-class swap — admin-configurable flat fee (default 50%) plus fare difference |
| No-show recovery (refund/reschedule) | `#F5A623` for refund, `#7C3AED` for reschedule | matching tints | Two-mode toggle within a single screen |
| Emergency cancellation (operator-initiated) | `#E63946` (= destructive) → `#7F1D1D` gradient | `#FEE2E2` | Operator-fault — emphasizes the issue |
| Travel credit / wallet            | `#7C3AED` action, `#5B21B6` ink | `#EDE9FE` border `#C4B5FD` | Long-term value held by customer |

### Status badge color map

Every booking status renders a `StatusBadge` pill. Phase 4 must keep this map literal:

| Status              | Foreground | Background | Notes                                          |
|---------------------|------------|------------|------------------------------------------------|
| Confirmed           | `#15803D` | `#DCFCE7` | Paid and ready to board                        |
| Pending Payment     | `#A16207` | `#FEF3C7` | Held seats, 15-min expiry                      |
| Used                | `#3730A3` | `#E0E7FF` | Boarded and sailed                             |
| Cancelled           | `#B91C1C` | `#FEE2E2` | Customer-initiated cancel                      |
| Refunded            | `#4B5563` | `#F3F4F6` | Xendit refund completed                        |
| No-Show             | `#92400E` | `#FEF3C7` | Marked on Boarding Officer's MC-180 manifest   |
| Rebooked            | `#6D28D9` | `#F0E7FF` | Source row for a pre-departure or no-show reschedule |
| Refund Pending      | `#A16207` | `#FEF3C7` | Awaiting Finance Manager processing            |
| Emergency Cancelled | `#B91C1C` | `#FEE2E2` | Operator-issued emergency cancellation; within 72h window |
| Credited            | `#5B21B6` | `#EDE9FE` | Auto-credit or customer-chosen credit recovery |

### Semantic alert colors (banners + toasts)

| Tone           | Border / icon | Background | Body text | Used for                                 |
|----------------|---------------|------------|-----------|------------------------------------------|
| Info           | `#1E40AF`     | `#EFF6FF`  | `#1E40AF` | Neutral helper text, "what to expect" notes |
| Success        | `#2A8364`     | `#DCFCE7`  | `#166534` | Check-in confirmations, ID-verified, refund success |
| Warning        | `#F5A623`     | `#FEF3C7`  | `#92400E` | Time-sensitive prompts, ID-verification pending, grace-period countdown |
| Destructive    | `#E63946`     | `#FEE2E2`  | `#7F1D1D` | Emergency cancellations, eligibility expired, ID mismatch |
| Purple (Credit/Reschedule no-show) | `#7C3AED` | `#EDE9FE` | `#5B21B6` | Travel credit, no-show reschedule, post-72h auto-credit fallback |

## Typography

| Property         | Value                                                                  |
|------------------|------------------------------------------------------------------------|
| Font family      | "Circular", Inter, system-ui, -apple-system, "Helvetica Neue", sans-serif |
| Fallback CDN     | https://rsms.me/inter/inter.css (Inter as primary fallback)            |
| Font weights     | 400 (regular), 500 (medium), 600 (semibold), 700 (bold)                |
| Base body size   | 16px (1rem)                                                            |
| Body line-height | 1.5                                                                    |
| Heading scale    | h1 32px / h2 26px / h3 22px / h4 18px / h5 16px / h6 14px              |
| Heading weight   | 600 (semibold) — Airbnb uses semibold over bold for warmth             |
| Heading line-height | 1.2                                                                 |
| Letter-spacing   | -0.01em on headings, 0 on body                                         |
| Small text       | 14px (captions, helper text, badges)                                   |
| Micro text       | 12px (timestamps, ID numbers, fine print only)                         |
| Micro mono       | 11-12px (booking refs, OTP codes, UniSMS message IDs, signature hashes) — must use a monospace stack (ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas) |

## Layout

| Property              | Value                                                       |
|-----------------------|-------------------------------------------------------------|
| Spacing unit          | 8px base — scale: 4, 8, 12, 16, 24, 32, 48, 64              |
| Border radius small   | 8px (inputs, small buttons, badges)                         |
| Border radius medium  | 12px (cards, modals, primary buttons)                       |
| Border radius large   | 16px (hero cards, photo containers, recovery-option cards)  |
| Border radius xl      | 24px (e-ticket card, success illustrations)                 |
| Border radius pill    | 9999px (filter chips, status pills, round avatars)          |
| Card padding          | 24px desktop / 16px mobile                                  |
| Card shadow rest      | 0 2px 4px rgba(0,0,0,0.08)                                  |
| Card shadow hover     | 0 6px 16px rgba(0,0,0,0.12)                                 |
| Sidebar width (admin) | 240px (collapsed: 64px icon-only)                           |
| Content max-width     | 1200px (admin); 1080px (customer marketing)                 |
| Booking flow max-width| 720px (centered, focused, mobile-first)                     |
| Recovery flow max-width| 640px (Refund / Reschedule / No-Show / Emergency / Credit) — narrower than booking flow to feel calm and focused |
| Admin emergency cancel max-width | 896px (4xl) — wider because the affected-bookings table needs the room |
| PWA phone-frame width | 360px (visualizes a 390-414px iPhone viewport with bezel)   |
| Header height         | 80px desktop / 64px mobile                                  |
| Button height primary | 48px (touch-friendly, Airbnb signature)                     |
| Button height compact | 40px (admin tables, dense UI)                               |
| Input height          | 56px on customer flow (large, friendly) / 40px in admin     |
| OTP digit cell        | 48×56px on mobile, 56×64px on desktop, monospace, font-bold |
| Focus ring            | 2px solid coral, offset 2px                                 |

## Component Patterns

### Buttons

| Component         | Specification                                                                  |
|-------------------|--------------------------------------------------------------------------------|
| Primary button    | bg-primary (coral) text-white rounded-md(12px) h-12 px-6 font-semibold hover:bg-primary-hover |
| Secondary button  | bg-secondary (ink) text-white rounded-md(12px) h-12 px-6 font-semibold         |
| Outline button    | bg-transparent border border-foreground text-foreground rounded-md(12px) h-12 px-6 font-semibold |
| Ghost button      | bg-transparent text-foreground rounded-md(12px) h-12 px-6 hover:bg-muted       |
| Destructive button| bg-destructive text-white rounded-md(12px) h-12 px-6 font-semibold — only used for emergency cancellation broadcast and similar irreversible operator actions |
| Hero CTA pill     | rounded-pill h-14 px-8 bg-primary text-white font-semibold flex items-center gap-2 |
| Mobile sticky CTA | fixed-bottom mobile-only: full-width bg-primary h-14 mx-4 mb-4 rounded-md(12px) |

### Inputs

| Component         | Specification                                                                  |
|-------------------|--------------------------------------------------------------------------------|
| Input             | h-14 (booking flow) / h-10 (admin) rounded-md(8px) border-input px-4 focus:ring-2 focus:ring-primary |
| OTP boxes         | 6-cell grid, each 48×56px (mobile) or 56×64px (desktop), monospace, big bold digit, focus ring coral; auto-advance on entry; backspace moves left |
| Phone input       | h-14 with `+63` prefix shown as a static prefix block; live E.164 normalization (`09xxxxxxxxx` → `+639xxxxxxxxx`); validates against `/^\+63\d{10}$/` |
| Signature pad     | Touch + mouse canvas, 320×120px on mobile, 480×160px on desktop, with "Clear" link button and a printed-name caption below — used twice on MC-180 manifest (Boarding Officer + Master) |
| Range slider (demo) | Native `<input type="range">` with `accent-color` matching the relevant flow color (coral for refund, destructive for emergency, etc.); always wrapped in a dashed-border `📐 Mockup control` card |

### Containers

| Component         | Specification                                                                  |
|-------------------|--------------------------------------------------------------------------------|
| Card              | rounded-md(12px) shadow-sm hover:shadow-md transition-shadow p-6 bg-card       |
| Filter chip       | rounded-pill px-4 py-2 border border-input text-sm hover:border-foreground (active: bg-foreground text-white) |
| Status badge      | rounded-pill px-3 py-1 text-xs font-medium (semantic color per Status badge color map) |
| Passenger-type pill | rounded-pill px-1.5 py-0.5 text-[10px] font-bold (color per Passenger-type pills table) — sits inline next to passenger name on counter manifest |
| Calendar day      | rounded-pill w-12 h-12 (available: hover:bg-muted; selected: bg-primary text-white; blocked: line-through muted-foreground) |
| PhoneFrame        | Stylized phone silhouette with notch + bezel; used in PWA Mobile Apps Preview to show Counter and Boarding PWAs side-by-side; coral or purple frame color depending on which PWA |

### Recovery & action panels

| Component                  | Specification                                                                  |
|----------------------------|--------------------------------------------------------------------------------|
| Recovery-mode toggle       | Segmented control, full-width, rounded-xl, p-1, bg-muted; each segment py-3 with icon+label; active segment gets white bg + small drop shadow + colored text. Used in No-Show Recovery (Refund / Reschedule) — Emergency Recovery uses three full-width *cards* instead |
| Recovery option card       | Full-width card, p-4, rounded-2xl, border-2; left icon tile (12×12 rounded-xl), title with badge ("100%" / "FREE" / "12 months"), one-line value-explanation, right-side `Check` when selected. Three of these stacked vertically in Emergency Recovery |
| Recovery summary card      | rounded-2xl p-5 border-2 with mode-themed tint and border; inside has a flex row with an icon + heading + body, then a nested white card with key-value rows (Original / New / Fee / Net) |
| Refund-ladder tier card    | rounded-xl p-3 border-2 with semantic tone (warning or destructive); single row: bold percent on left, time range on right |
| Compliance banner          | rounded-xl p-3 mb-3 border-2 with semantic-tone bg; icon tile (9×9 rounded-lg) on left, two-line text on right (bold headline + body). Two variants: warning (amber, ID verification pending / time-sensitive) and destructive (red, emergency cancellation) |
| Demo control strip         | rounded-xl p-3 border-2 *border-dashed* (the dashed border is what distinguishes mockup-only UI from real); always prefixed with `📐 Mockup control` |
| Countdown chip             | Small rounded-lg p-2 inside a banner, with a `Clock` icon and "Nh left to choose / pick recovery" copy; turns from amber to red as deadline approaches |
| Travel-credit balance card | rounded-2xl p-5 border-2 in `#EDE9FE` bg / `#7C3AED` border; massive 4xl/5xl `₱X,XXX` figure in `#5B21B6`; "Total available credit" caption above; "Book a trip with credit →" CTA below in `#7C3AED` filled button |
| SMS ticket preview (phone frame) | Rendered on ConfirmationScreen as a stylized phone frame (rounded-3xl, dark bezel `#1a1a1a`, maxWidth 360px) containing a simulated iMessage-style SMS conversation from sender ID `FSMARINE`. Two SMS bubbles: (1) booking confirmation with ref, date, route, vessel, class, seats, pax count, amount paid; (2) ID reminder per passenger listing required physical IDs with discount forfeiture warning. Gray SMS bubble style (`#E9E9EB`), "Delivered ✓" status under each. Phone frame includes a faux status bar (carrier, notch, time) and iMessage input bar at bottom. Below the frame: "SMS sent from sender ID FSMARINE to +63 9XX XXX XXXX via UniSMS" caption |
| Duplicate scan alert (counter) | rounded-2xl p-4 border-2 in `#FEE2E2` bg / destructive border; red circle icon (AlertTriangle white on `#E63946`); bold "⚠ DUPLICATE SCAN — already checked in" heading in `#7F1D1D`; shows passenger name, seat, ref, plus a white card with "This ticket was already scanned" detail (checked-in-at time and staff name), warning text that the person presenting may not be the original passenger. Two actions: "Dismiss alert" (neutral outline) and "Report to supervisor" (destructive filled). Triggered when a QR code for an already-checked-in passenger is scanned again |
| Duplicate scan alert (gangway) | Same styling as counter variant but heading says "⚠ DUPLICATE SCAN — already boarded"; detail says "This passenger already boarded the vessel" with boarded-at time; warning says person is NOT the original passenger and may be attempting unauthorized boarding. Primary action: "Deny boarding · report" (destructive filled). Triggered when a QR code for an already-boarded passenger is scanned at the gangway |

### Booking-flow patterns

| Component                  | Specification                                                                  |
|----------------------------|--------------------------------------------------------------------------------|
| Step indicator             | Horizontal flex row of 6 steps (Date · Sailing · Passengers · Seats · Review · Pay), separated by `ChevronRight` icons, text-sm, ink-muted color; the current step is `font-semibold` in `COLORS.primary` (coral); completed steps get a trailing `✓`. Wraps on narrow viewports via `flex-wrap`. Step count is locked at 6 across all booking-flow screens for consistency. Present on: CalendarScreen (step 1), SailingsListScreen (step 2), ClassPickerScreen (step 2), PassengersScreen (step 3), SeatSelectionScreen (step 4), ReviewScreen (step 5). Step 2 label is always "Sailing" (class picking is a sub-step of sailing selection, not its own step). ClassPickerScreen uses step 2 active because it's still within the sailing selection flow |
| Seat-number pill           | Inline `font-mono text-xs font-semibold px-1.5 py-0.5 rounded`, background class-tint (Aircon `#FFE5E9`), text class-foreground (`COLORS.primary` for Aircon). Shows canonical seat ID (e.g. `A03-B`). Appears on: ReviewScreen (per-passenger row), ConfirmationScreen (e-ticket passenger list), BookingDetailScreen (passenger table). Format is always `{ClassPrefix}{Row:2d}-{ColLetter}` — e.g. `A03-B` for Aircon row 3 seat B, `O07-F` for Open Air row 7 seat F, `V02-C` for VIP row 2 seat C |
| Account Owner pill         | Small `rounded-pill px-2 py-0.5 text-[10px] font-bold uppercase tracking-wide`; background `#FFE5E9` (coral tint), text `#FF385C` (= primary); reads "Account owner"; appears inline beside the "Passenger 1" heading on the Passenger Forms screen. The passenger card itself gets a 2px coral border (vs the standard 1px neutral border on the other passengers) to visually anchor Passenger 1 as special |
| Linked-account hint        | rounded-lg p-2 mt-1.5 text-xs, background `#DCFCE7` (success tint), text `#166534` (success ink), `ShieldCheck` icon on left. Appears under the Contact Number field on Passenger 1 *only* when the typed phone matches a known account. Copy template: "This number is already linked to an account. We'll attach this booking to it automatically — no sign-in needed." |
| Seat map · seat cell       | Square button `w-8 h-8` (mobile) / `w-9 h-9` (desktop), `rounded-md`, `border-2`, `text-[10px]` / `text-xs`, `font-bold`. Three states: **Available** (`bg-white`, `border-COLORS.border`, ink text), **Selected** (`bg-{classTheme}` filled, white text — class-themed coral for Aircon, blue for Open Air, amber for VIP), **Occupied** (`bg-#E4E4E4`, `border-#D1D5DB`, `text-#9CA3AF`, opacity 0.6, `cursor-not-allowed`, X icon replacing seat letter). Letter shown is the column letter (A–H per row); the full seat ID (`A05-C`) lives in the `title` attribute and the passenger-assignment chip |
| Seat map · row             | Horizontal flex row, gap-1.5, mb-1.5; leading row label is a `w-6 text-right text-[10px] font-mono` 2-digit zero-padded number (`01`–`10`); seat cells render across in column order. Aisle gap is a `w-3` / `w-4` empty spacer with a centered `·` glyph in ink-muted — visually breaks the row without occupying a real seat slot |
| Seat map · class layout    | Three layouts hard-coded into the screen config and matched by the Seat entity's class capacity contract: **Open Air** = 10 rows × 8 cols, no aisle (bench style), 80 seats, IDs `O01-A` to `O10-H`. **Aircon** = 10 rows × 5 cols, aisle after column 3 (between C and D), 50 seats, IDs `A01-A` to `A10-E`. **VIP** = 3 rows × 4 cols, aisle after column 2 (between B and C), 12 seats, IDs `V01-A` to `V03-D`. Class never changes within this screen — it's locked from the previous step. A demo-strip class toggle is present in the mockup ONLY |
| Seat map · bow/stern chips | Centered `rounded-pill px-3 py-1 text-[10px] uppercase tracking-widest`, background `COLORS.bgMuted`, text `COLORS.inkMuted`; bow chip has an `Anchor` icon and reads "Bow · front of vessel"; stern chip reads "Stern · rear". Frame the seat grid so the customer knows which way they're facing |
| Passenger-seat chip        | Full-width row, `rounded-lg p-2.5`, `border`; left side: numbered circle (`w-7 h-7 rounded-full`, color-coded to selection state — class-theme filled if seat picked, amber filled if this is the next-to-fill, neutral if not yet up) + passenger full name truncated. Right side: when assigned, seat ID in class-theme bold font-mono + a small `X` button to unassign; when unassigned, helper text ("Tap a seat below →" for the next-to-fill, "Not yet picked" for later passengers) |
| Seat-availability legend   | Horizontal flex row above the seat map, gap-4, text-xs ink-muted; three squares (Available white / Your pick class-themed / Taken gray with X icon) plus a right-aligned "N/N free" count in font-mono. Updates live as the customer picks |

## Iconography

| Property    | Value                                                                  |
|-------------|------------------------------------------------------------------------|
| Library     | lucide-react (shadcn/ui default — locked by framework) — pinned at 0.460.x for legacy icon-alias stability |
| Default size| 20px in body, 16px in compact UI, 24px in hero/empty states            |
| Stroke      | 2px (lucide default)                                                   |
| Color       | inherits from text color (currentColor)                                |
| Booking flow icons | Ship, Calendar, Clock, Users, CreditCard, CheckCircle2, Sunrise, Sun, Sunset (time-of-day) |
| Confirmation method icons | Mail, Phone, MessageSquare (for the SMS-vs-Email picker)        |
| OTP / SMS icons | KeyRound, RefreshCw, ShieldCheck, AlertCircle (UniSMS error states) |
| Admin icons | LayoutDashboard, Anchor, CalendarRange, Ban, BadgePercent, Wallet, BarChart3, Users, Settings, FileText, ScrollText |
| Boarding Officer icons | ScanLine, ShieldCheck, AlertTriangle, FileSpreadsheet (MC-180), Edit3 (signature) |
| Discount-ID verification icons | ShieldCheck (verify), Check (ID matches), X (ID mismatch) |
| Recovery flow icons | Wallet (refund), CalendarRange (reschedule), Banknote (travel credit), ArrowRightLeft (rebook), RefreshCw (no-show recovery), AlertTriangle (emergency), Clock (countdown) |
| Travel credit wallet icons | Banknote (main), Wallet (alternate), TrendingUp (used / progress) |
| Seat selection icons | Anchor (bow chip), X (occupied cell + unassign button), Sun (Open Air class toggle), Snowflake (Aircon class toggle), Crown (VIP class toggle), ShieldCheck (linked-account hint) |

## Photography & Imagery Direction

| Surface              | Direction                                                              |
|----------------------|------------------------------------------------------------------------|
| Landing hero         | Wide aspirational shot — vessel at sea between Batangas mainland and Lubang Island at sunrise/sunset; warm tones; aspect 16:9 desktop, 4:5 mobile |
| Class showcase cards | Square (1:1) — Open Air deck with passengers, Aircon interior cabin, VIP suite with privacy curtain |
| Vessel cards (admin) | Square thumbnail (1:1) — operator-uploaded fleet photos                |
| Empty states         | Lightweight illustration in coral + ink tones — no stock photography   |
| QR code on e-ticket  | Black on white, 200×200px minimum, with white-bg padding for scanability |
| Emergency cancellation broadcast | NO imagery — destructive-tone banners only; this is a serious operational action, not a marketing surface |
| Travel credit wallet | NO photography — `Banknote` icon + bold typography only; communicates "money" without literal currency imagery |

## Voice & Tone

| Surface            | Tone                                                                     |
|--------------------|--------------------------------------------------------------------------|
| Customer-facing    | Friendly, concise, second-person ("Pick your date"); never corporate     |
| Hero copy          | Aspirational but plain-spoken — "Book your trip to Lubang Island in just a minute." Never make claims the product can't deliver (e.g. don't say "ferry crossing in 60 seconds" when the crossing is 4 hours — refer to *booking* time, not *travel* time) |
| Confirmation messages | Warm and reassuring ("You're booked! See you at the pier.")           |
| Error messages     | Direct and helpful — what failed + what to do next; never blame the user |
| Recovery flows (refund / reschedule / no-show / emergency) | Empathetic but action-oriented; acknowledge the disruption without dwelling ("Your sailing was cancelled" → "Pick how you'd like to recover — your booking value is fully protected") |
| Seat selection | Concrete and visual ("Pick your seats" → "Pick N more seats" → "Continue · A05-C, A05-D, A05-E →"). Never abstract ("Select seating preferences"). The button label always shows actual seat IDs when complete so the customer can verify before paying |
| Account-owner explainer | Plain-spoken about the silent link — "This name plus your contact phone identifies your account. If a phone number you enter below is already linked to an F&S Marine account, this booking will attach to that account automatically — no separate sign-in needed." Never use security/identity jargon ("authentication", "credentials"); never imply the customer must do something extra |
| Discount-ID reminders | Frank but kind ("Bring the right ID for each passenger" — never "WARNING:"); explain consequences without scolding |
| Counter staff prompts | Procedural; use imperative voice ("Ask passenger for: Senior Citizen ID"); brief because staff are mid-task |
| Boarding Officer / MC-180 | Formal, regulatory — this is a legal document. "Master/Captain" not "Captain". Cite MARINA / PCG license numbers verbatim |
| Admin UI           | Neutral and professional; assumes domain literacy (no over-explaining)   |
| Emergency cancellation broadcast preview | Sober and unambiguous — "This action cannot be undone." Always specify customer count, max liability, and the recovery options being offered |
| Email templates    | Personal opener ("Hi Maria,") — never "Dear Customer"                    |
| SMS templates      | Maximum 160 chars; lead with brand name ("F&S Marine:"); include reference + key facts only; never marketing copy in an OTP or refund SMS |

## Reference formats (locked in UI)

These string formats appear verbatim across the UI and are documented here so Phase 4 type definitions match exactly:

| Ref kind                            | Format                  | Example                  | Issued when                          |
|-------------------------------------|-------------------------|--------------------------|--------------------------------------|
| Booking                             | `FSM-YYYY-MMDD-XXXX`    | `FSM-2026-0518-7K2A`     | At payment + confirmation step       |
| Pre-departure refund request        | `RR-YYYY-MMDD-XXXX`     | `RR-2026-0519-3M2K`      | Customer requests refund ≥24h pre-departure |
| No-show refund request              | `NSR-YYYY-MMDD-XXXX`    | `NSR-2026-0519-4B8M`     | Customer requests refund within 120h of manifest finalization |
| No-show reschedule (new booking)    | `NSB-YYYY-MMDD-XXXX`    | `NSB-2026-0519-7H2C`     | Customer reschedules a no-show within grace |
| Emergency cancellation announcement | `EMC-YYYY-MMDD-XXXX`    | `EMC-2026-0521-7K2M`     | Operations Manager broadcasts cancellation |
| Emergency refund                    | `EMR-YYYY-MMDD-XXXX`    | `EMR-2026-0521-4B8M`     | Customer chooses Refund on Emergency Recovery |
| Travel credit                       | `CRD-YYYY-MMDD-XXXX`    | `CRD-2026-0521-9V2K`     | Customer chooses Credit OR auto-credit cron runs |
| PH phone (E.164)                    | `+63 9XX XXX XXXX`      | `+63 917 845 2103`       | Stored E.164, displayed with spaces  |
| UniSMS sender ID                    | `FSMARINE`              | (literal)                | Locked — change requires UniSMS re-approval |
| UniSMS message ID                   | `msg_<uuid>`            | `msg_84e8b93b-6315-46af` | Returned by POST /otp; shown in toasts for support |
| Voyage ID                           | `FSM-V-YYYY-MM-DD-NNN`  | `FSM-V-2026-05-22-001`   | Cron-generated nightly                |

Refs always render in monospace, with the prefix in primary color (or status-mode color) and the rest in ink. Booking refs (and only booking refs) appear in QR codes.

## Theming Reference
- shadcn/ui theming: https://ui.shadcn.com/docs/theming
- Dark mode: https://ui.shadcn.com/docs/dark-mode (optional toggle, light is default)
- Source design: https://getdesign.md/airbnb/design-md
- Live preview: https://getdesign.md/design-md/airbnb/preview.html

## Phase 4 Implementation Note (for Claude Code)

After Phase 3 generates `inputs.yml`, run from project root:

```bash
npx getdesign@latest add airbnb
```

This installs the base official Airbnb DESIGN.md tokens into the project. This file (`docs/DESIGN.md`) is the Phase 2.8 extraction *plus* the locked architectural colors (class, port, role, passenger-type, recovery-mode, status badge) that emerged through Batches 7-13 — the npx-installed file does NOT include those, so this document is authoritative for anything beyond the base palette.

If the two ever conflict on the base palette (background, foreground, primary, ink, success, warning, destructive, border, muted), prefer the npx-installed `DESIGN.md` at project root for runtime token values. For everything else — class colors, port colors, role colors, status badge map, passenger-type pills, recovery-mode color conventions, semantic alert tones, reference formats, component patterns specific to F and S Marine, voice & tone — this file is canonical and must be honored.

Locked tokens that Phase 4 must NOT reassign:
- Open Air class = `#1E40AF` foreground / `#DBEAFE` background
- Aircon class = `#FF385C` (= primary) foreground / `#FFE5E9` background
- VIP class = `#A16207` foreground / `#FEF3C7` background
- Boarding Officer role = `#7C3AED` foreground / `#EDE9FE` background
- Travel credit theme = `#7C3AED` action / `#5B21B6` ink / `#EDE9FE` tint
- Emergency cancellation = `#E63946` (= destructive) → `#7F1D1D` gradient
- BAT-NAS port = coral (primary), BAT-CAL port = amber (`#A16207`), MIN-TIL = gray
- Status badge color map (10 statuses) — verbatim per the table above
- All reference-format prefixes (`FSM-`, `RR-`, `NSR-`, `NSB-`, `EMC-`, `EMR-`, `CRD-`)
- Seat capacity contract: Open Air = 80 (10×8, IDs `O01-A` through `O10-H`); Aircon = 50 (10×5, IDs `A01-A` through `A10-E`); VIP = 12 (3×4, IDs `V01-A` through `V03-D`). Aisle convention: Aircon has aisle between columns C and D; VIP has aisle between columns B and C; Open Air has no aisle. The Seat entity in PRODUCT.md derives from these locked counts — changing them requires re-issuing all future Voyage seat rows and a vessel-config migration

These weren't free decisions — they encode domain meaning the rest of the app depends on (e.g. a passenger seeing a purple background instantly knows it's about credits, not about a no-show; an admin seeing destructive red on a banner knows the action is irreversible). Reassigning them will silently break the UX even though the code still compiles.
